# KindShare 注册后获得代币流程分析

## 概述
用户注册后获得代币的完整流程涉及**前端 → 后端 → Web3 区块链** 三个层次的交互。以下是详细的代码执行先后顺序和逻辑。

---

## 📋 完整流程时序图

```
用户点击注册
    ↓
[前端] Register.jsx 提交注册数据
    ↓
[API] authApi.register() → POST /api/v1/register
    ↓
[后端] registerUser() 创建用户
    ↓
[后端] sendToken() 返回 JWT Token
    ↓
[前端] userApi.getMe() 获取用户信息
    ↓
用户跳转到首页
    ↓
[用户操作] 连接钱包 (connectWallet)
    ↓
[API] web3Api.connectWallet() → POST /api/v1/wallet/connect
    ↓
[后端] connectWallet() 验证钱包地址
    ↓
[Web3] mintTokensForNewUser() 铸造 100 代币
    ↓
[区块链] 交易完成，MetaMask 显示新代币
```

---

## 🔑 关键步骤详解

### 第一步：前端用户注册

**文件：** `frontend/src/components/auth/Register.jsx`

```javascript
const submitHandler = (e) => {
  e.preventDefault();
  const signUpData = {
    name,
    email,
    password,
  };
  register(signUpData);  // 调用 Redux 的 registerMutation
};
```

**执行逻辑：**
- 用户填写 name、email、password
- 点击"REGISTER"按钮
- 触发 `register()` mutation

---

### 第二步：调用 Redux Auth API

**文件：** `frontend/src/redux/api/authApi.js`

```javascript
register: builder.mutation({
  query(body) {
    return {
      url: "/register",
      method: "POST",
      body,
    };
  },
  async onQueryStarted(args, { dispatch, queryFulfilled }) {
    try {
      await queryFulfilled;
      // ✨ 关键：注册成功后立即调用 getMe() 获取用户信息
      await dispatch(userApi.endpoints.getMe.initiate(null));
    } catch (error) {
      console.log(error);
    }
  },
}),
```

**执行逻辑：**
- 发起 POST 请求到 `/api/v1/register`
- 注册成功后，**自动触发** `userApi.getMe()` 获取当前用户信息
- 将用户信息存储到 Redux store

---

### 第三步：后端处理注册

**文件：** `backend/controllers/authControllers.js`

```javascript
export const registerUser = catchAsyncErrors(async (req, res, next) => {
  const { name, email, password } = req.body;

  // 创建新用户
  const user = await User.create({
    name,
    email,
    password,
  });

  // 生成 JWT Token 并返回
  sendToken(user, 201, res);
});
```

**执行逻辑：**
1. 从请求体中提取 name、email、password
2. 在 MongoDB 中创建新用户记录
3. **注意：此时还没有连接钱包或铸造代币**

---

### 第四步：发送 JWT Token

**文件：** `backend/utils/sendToken.js`

```javascript
export default (user, statusCode, res) => {
  // 创建 JWT Token
  const token = user.getJwtToken();

  // 设置 Cookie 选项
  const options = {
    expires: new Date(
      Date.now() + process.env.COOKIE_EXPIRES_TIME * 24 * 60 * 60 * 1000
    ),
    httpOnly: true,
  };

  res.status(statusCode).cookie("token", token, options).json({
    token,
  });
};
```

**执行逻辑：**
- 使用用户 ID 生成 JWT Token
- 将 Token 存储在 HTTP-Only Cookie 中
- 返回 Token 给前端

---

### 第五步：获取当前用户信息

**文件：** `backend/routes/auth.js`

```javascript
router.route("/me").get(isAuthenticatedUser, getUserProfile);
```

**文件：** `backend/controllers/authControllers.js`

```javascript
export const getUserProfile = catchAsyncErrors(async (req, res, next) => {
  const user = await User.findById(req?.user?._id);

  res.status(200).json({
    user,
  });
});
```

**执行逻辑：**
- 验证 JWT Token
- 获取当前登录用户的完整信息
- 返回用户对象（包括 walletAddress、tokenBalance 等字段）

**用户模型：** `backend/models/user.js`

```javascript
const userSchema = new mongoose.Schema({
  name: String,
  email: String,
  password: String,
  role: {
    type: String,
    enum: ["user", "admin"],
    default: "user",
  },
  walletAddress: {  // 钱包地址（注册时为 null）
    type: String,
    default: null,
    unique: true,
    sparse: true,
  },
  tokenBalance: {   // 代币余额（注册时默认 100）
    type: Number,
    default: 100
  },
  // ...其他字段
});
```

---

### 第六步：用户连接钱包 ⭐ **（这是铸造代币的触发点）**

**文件：** `frontend/src/components/user/Profile.jsx` 或其他页面

用户在某个页面（如用户资料页面或首页）点击"连接钱包"按钮

```javascript
// 前端调用
const [connectWallet] = useConnectWalletMutation();

const handleConnectWallet = async () => {
  const account = await connectWallet({ walletAddress: userAccount });
};
```

**前端 API：** `frontend/src/redux/api/web3Api.js`

```javascript
connectWallet: builder.mutation({
  query: (data) => ({
    url: '/wallet/connect',
    method: 'POST',
    body: data,
  }),
}),
```

---

### 第七步：后端钱包连接处理 ⭐ **（铸造代币的核心逻辑）**

**文件：** `backend/controllers/web3Controllers.js`

```javascript
export const connectWallet = catchAsyncErrors(async (req, res, next) => {
  const { walletAddress } = req.body;

  // 1️⃣ 验证钱包地址格式
  if (!walletAddress) {
    return next(new ErrorHandler("Wallet address is required", 400));
  }

  if (!isValidEthereumAddress(walletAddress)) {
    return next(new ErrorHandler("Invalid wallet address format", 400));
  }

  // 2️⃣ 检查钱包是否已被其他用户使用
  const existingUser = await User.findOne({ walletAddress });
  if (existingUser && existingUser._id.toString() !== req.user._id.toString()) {
    return next(new ErrorHandler("This wallet is already connected to another account", 400));
  }

  // 3️⃣ 更新用户的钱包地址
  const user = await User.findByIdAndUpdate(
    req.user._id,
    { walletAddress },
    { new: true }
  );

  // ✨ 4️⃣ 关键：为用户铸造初始代币（100个）
  const mintResult = await mintTokensForNewUser(walletAddress, 100);

  if (!mintResult.success) {
    console.warn("Warning: Token minting failed, but wallet connected:", mintResult.error);
  }

  res.status(200).json({
    success: true,
    user,
    message: "Wallet connected successfully"
  });
});
```

**执行逻辑流程：**
1. 验证用户发送的钱包地址
2. 检查地址格式是否合法（ERC-20 标准地址）
3. 确保钱包地址没有被其他用户绑定
4. **将钱包地址保存到 MongoDB 的 User 文档中**
5. **调用 `mintTokensForNewUser()` 铸造代币**

---

### 第八步：Web3 服务铸造代币 ⭐ **（与区块链交互）**

**文件：** `backend/utils/web3Service.js`

```javascript
export const mintTokensForNewUser = async (userWalletAddress, amount = 100) => {
  try {
    // 1️⃣ 获取智能合约实例
    const contract = getContract();
    
    // 2️⃣ 将金额转换为 Wei 单位（ERC-20 标准，18 位小数）
    const amountInWei = ethers.parseUnits(amount.toString(), 18);
    
    console.log(`Minting ${amount} tokens to ${userWalletAddress}`);
    
    // 3️⃣ 调用智能合约的 mint() 函数
    const tx = await contract.mint(userWalletAddress, amountInWei);
    
    // 4️⃣ 等待区块链上的交易确认
    const receipt = await tx.wait();
    
    console.log(`Mint transaction hash: ${receipt.hash}`);
    
    // 5️⃣ 返回交易成功信息
    return {
      success: true,
      transactionHash: receipt.hash
    };
  } catch (error) {
    console.error("Mint error:", error);
    return {
      success: false,
      error: error.message
    };
  }
};
```

**执行逻辑：**
1. 从 Web3 Config 获取初始化的智能合约实例
2. 将要铸造的代币数量（100）转换为 Wei 单位
3. 调用智能合约的 `mint()` 方法
4. 等待区块链交易被挖矿确认
5. 返回交易哈希给前端

---

### 第九步：Web3 配置和合约初始化

**文件：** `backend/config/web3Config.js`

```javascript
import { ethers } from "ethers";
import dotenv from "dotenv";

dotenv.config({ path: "backend/config/config.env" });

const SEPOLIA_RPC_URL = process.env.SEPOLIA_RPC_URL;      // Sepolia 测试网 RPC 链接
const CONTRACT_ADDRESS = process.env.CONTRACT_ADDRESS;    // KindToken 合约地址
const PRIVATE_KEY = process.env.PRIVATE_KEY;              // 后端钱包私钥（铸造权限）

// ERC-20 ABI (智能合约函数定义)
const CONTRACT_ABI = [
  "function transfer(address to, uint256 amount) public returns (bool)",
  "function transferFrom(address from, address to, uint256 amount) public returns (bool)",
  "function balanceOf(address account) public view returns (uint256)",
  "function mint(address to, uint256 amount) public",  // ✨ 关键：铸造函数
  "function burn(uint256 amount) public"
];

// 初始化 Web3 连接
let provider;
let wallet;
let contract;

try {
  if (SEPOLIA_RPC_URL && CONTRACT_ADDRESS && PRIVATE_KEY) {
    // 1️⃣ 创建提供者（连接到 Sepolia 测试网）
    provider = new ethers.JsonRpcProvider(SEPOLIA_RPC_URL);
    
    // 2️⃣ 创建钱包（使用后端私钥，拥有铸造权限）
    wallet = new ethers.Wallet(PRIVATE_KEY, provider);
    
    // 3️⃣ 创建合约实例（连接后端钱包）
    contract = new ethers.Contract(CONTRACT_ADDRESS, CONTRACT_ABI, wallet);
  }
} catch (error) {
  console.error("Web3 Configuration Error:", error.message);
}

export const getContract = () => {
  if (!contract) {
    throw new Error("Web3 contract not initialized. Check your environment variables.");
  }
  return contract;
};
```

**执行逻辑：**
- 从环境变量获取 Sepolia RPC URL、合约地址、后端私钥
- 初始化 ethers.js 提供者连接到 Sepolia 测试网络
- 使用后端私钥创建签名者（钱包），拥有代币铸造权限
- 创建合约实例，连接合约地址和 ABI

**环境变量：** `backend/config/config.env`

```env
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY
CONTRACT_ADDRESS=0x...  # KindToken 合约地址
PRIVATE_KEY=0x...       # 后端钱包私钥（部署者钱包）
```

---

### 第十步：区块链交易确认

**区块链上发生：**
1. 后端使用私钥签名 `mint()` 交易
2. 交易被广播到 Sepolia 测试网络
3. 区块链验证者验证交易
4. 交易被挖矿包含在区块中
5. 100 个 KindToken 被铸造到用户的 MetaMask 钱包
6. 交易哈希返回给后端

---

### 第十一步：前端更新钱包信息

**用户在 MetaMask 中看到：**
- MetaMask 弹出交易确认窗口
- 交易成功后，钱包余额增加 100 KindToken
- 区块浏览器可以查看交易详情

---

## 🔄 完整数据流

### 数据库 (MongoDB)

```javascript
// 用户集合变化
// 注册前：
不存在

// 注册后（第一步）：
{
  _id: ObjectId(...),
  name: "张三",
  email: "user@example.com",
  password: "hashed_password",
  role: "user",
  walletAddress: null,          // ❌ 还没有钱包
  tokenBalance: 100,            // 默认值
  createdAt: "2025-11-12T...",
  updatedAt: "2025-11-12T..."
}

// 连接钱包后（第六步）：
{
  _id: ObjectId(...),
  name: "张三",
  email: "user@example.com",
  password: "hashed_password",
  role: "user",
  walletAddress: "0x742d35Cc6634C0532925a3b844Bc9e7595f...",  // ✅ 已保存
  tokenBalance: 100,
  createdAt: "2025-11-12T...",
  updatedAt: "2025-11-12T..."
}
```

### 区块链 (Sepolia)

```javascript
// 智能合约状态变化

// 铸造前：
balanceOf(0x742d35Cc6634C0532925a3b844Bc9e7595f...) = 0

// 铸造后（第八、九、十步）：
balanceOf(0x742d35Cc6634C0532925a3b844Bc9e7595f...) = 100 * 10^18 Wei
                                                     = 100 KindToken

// 交易记录：
{
  from: "0x...后端钱包",
  to: "0x742d35Cc6634C0532925a3b844Bc9e7595f...",
  value: 0,
  data: "mint(...)",
  transactionHash: "0x...",
  blockNumber: 12345678,
  status: "success"
}
```

---

## 📊 关键函数调用关系

```
Register.jsx (前端)
  ↓
authApi.register() 
  ↓
registerUser() [后端]
  ↓
User.create()
  ↓
sendToken()
  ↓
[前端收到 JWT Token]
  ↓
userApi.getMe() [自动触发]
  ↓
getUserProfile() [后端]
  ↓
[用户看到已登录]
  ↓
[用户点击连接钱包]
  ↓
web3Api.connectWallet()
  ↓
connectWallet() [后端]
  ↓
User.findByIdAndUpdate()
  ↓
mintTokensForNewUser() [Web3 Service]
  ↓
getContract()
  ↓
contract.mint() [智能合约]
  ↓
[交易被发送到 Sepolia]
  ↓
tx.wait() [等待确认]
  ↓
[返回交易哈希]
  ↓
[MetaMask 显示新代币]
```

---

## 🎯 核心要点总结

| 步骤 | 执行位置 | 操作 | 结果 |
|------|--------|------|------|
| 1️⃣ | 前端 | 用户提交注册表单 | 发起 POST /api/v1/register |
| 2️⃣ | Redux | authApi 拦截请求 | 自动触发 userApi.getMe() |
| 3️⃣ | 后端 | registerUser() | 在 MongoDB 创建用户 |
| 4️⃣ | 后端 | sendToken() | 返回 JWT Token |
| 5️⃣ | 后端 | getUserProfile() | 返回用户信息（walletAddress: null） |
| 6️⃣ | 前端 | 用户连接 MetaMask | 调用 connectWallet() |
| 7️⃣ | 后端 | connectWallet() | 验证并保存钱包地址 |
| 8️⃣ | Web3 | mintTokensForNewUser() | 构造 mint 交易 |
| 9️⃣ | 区块链 | contract.mint() | 铸造 100 代币到用户钱包 |
| 🔟 | 前端 | MetaMask | 显示新增加的 100 代币 |

---

## ⚠️ 重要注意点

1. **注册不会立即铸造代币**
   - 只有在用户连接 MetaMask 钱包时才会铸造
   - 这样的设计避免了为从未使用过钱包的用户浪费 Gas

2. **钱包地址唯一性**
   - 同一个钱包不能被多个账户绑定
   - 同一个账户只能绑定一个钱包

3. **Web3 Config 依赖环境变量**
   - `SEPOLIA_RPC_URL`：Sepolia 测试网 RPC 端点
   - `CONTRACT_ADDRESS`：已部署的 KindToken 合约地址
   - `PRIVATE_KEY`：拥有 mint 权限的后端钱包私钥

4. **Gas 费用**
   - 所有交易费用由后端钱包支付
   - 使用 Sepolia 测试网络，不需要真实资金

5. **交易异步性**
   - `mint()` 调用后需要等待 `tx.wait()`
   - 交易可能在几秒到几分钟内被确认

---

## 🔗 相关文件路径

```
前端：
├── src/components/auth/Register.jsx ..................... 注册表单
├── src/components/user/Profile.jsx ...................... 用户资料（可能有钱包连接）
├── src/hooks/useWeb3.js ................................ Web3 Hook
├── src/redux/api/authApi.js ............................. 认证 API
└── src/redux/api/web3Api.js ............................. Web3 API

后端：
├── controllers/authControllers.js ....................... registerUser()
├── controllers/web3Controllers.js ....................... connectWallet()
├── utils/web3Service.js ................................. mintTokensForNewUser()
├── config/web3Config.js ................................. Web3 初始化
├── models/user.js ........................................ 用户模型（walletAddress、tokenBalance）
├── utils/sendToken.js .................................... JWT Token 生成
└── routes/auth.js & web3.js .............................. 路由定义
```

---

## 🚀 流程总结

**简言之，注册后获得代币的完整流程为：**

1. **用户注册** → 创建账户（无代币）
2. **注册成功** → 获得 JWT Token
3. **连接钱包** → 后端验证钱包地址
4. **铸造代币** → 后端使用私钥调用智能合约
5. **区块链确认** → 100 代币转账到用户 MetaMask
6. **用户可见** → MetaMask 显示新代币

关键的触发点是**第 6 步"连接钱包"**，而不是注册本身。

