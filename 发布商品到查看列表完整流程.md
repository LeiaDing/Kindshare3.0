# 登录用户发布商品到查看所有商品 - 完整流程分析

## 概述

本文档详细解析用户从注册登录、发布商品到查看自己发布的所有商品的完整流程。

**注意:** 当前系统设计中，发布商品功能仅限**admin角色**使用，普通用户暂无此功能。本文档按照当前架构分析。

---

## 📋 系统架构现状

```
用户角色:
├─ 普通用户 (role: "user")
│  ├─ 可以浏览所有商品 ✅
│  ├─ 可以购买商品 ✅
│  ├─ 可以发布商品 ❌ (需要 admin 角色)
│  └─ 可以查看自己的订单 ✅
│
└─ 管理员 (role: "admin")
   ├─ 可以浏览所有商品 ✅
   ├─ 可以发布商品 ✅
   ├─ 可以查看所有已发布商品 ✅
   └─ 可以编辑/删除商品 ✅
```

---

## 🔄 完整流程示图

```
┌────────────────────────────────────┐
│  1️⃣  用户注册                       │
│  (用户创建账户)                     │
└────────────┬───────────────────────┘
             │
             ▼
┌────────────────────────────────────┐
│  2️⃣  用户登录                       │
│  (获得 JWT Token)                  │
└────────────┬───────────────────────┘
             │
             ▼
┌────────────────────────────────────┐
│  3️⃣  检查用户角色                   │
│  (role = "user" 还是 "admin"?)     │
└────────────┬───────────────────────┘
             │
     ┌───────┴───────┐
     │               │
 role="user"    role="admin"
     │               │
     ▼               ▼
┌──────────┐   ┌──────────────────────┐
│  普通用户 │   │  4️⃣ 进入管理后台    │
│ 无法发布  │   │ (访问 /admin/)       │
└──────────┘   └────────┬─────────────┘
                        │
                        ▼
              ┌──────────────────────┐
              │  5️⃣  点击"发布商品"   │
              │ (/admin/product/new) │
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  6️⃣  填写商品信息     │
              │ • 名称               │
              │ • 描述               │
              │ • 价格 (代币数)       │
              │ • 类别               │
              │ • 库存               │
              │ • 卖家名称           │
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  7️⃣  点击"CREATE"    │
              │ 提交商品数据          │
              └────────┬─────────────┘
                       │
         POST /api/v1/admin/products
                       │
                       ▼
              ┌──────────────────────┐
              │  8️⃣  后端验证        │
              │ • 检查 JWT Token     │
              │ • 检查是否 admin     │
              │ • 验证必填字段       │
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  9️⃣  保存到数据库    │
              │ Product.create()     │
              │ • user = req.user._id│
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  🔟 返回给前端        │
              │ HTTP 200 + product   │
              │ 自动跳转到商品列表    │
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  1️⃣1️⃣ 进入商品列表页面  │
              │ (/admin/products)    │
              └────────┬─────────────┘
                       │
         GET /api/v1/admin/products
                       │
                       ▼
              ┌──────────────────────┐
              │  1️⃣2️⃣ 后端查询        │
              │ Product.find()       │
              │ (所有商品)            │
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  1️⃣3️⃣ 返回商品数组    │
              │ 包含编辑/删除按钮    │
              └────────┬─────────────┘
                       │
                       ▼
              ┌──────────────────────┐
              │  1️⃣4️⃣ 前端显示列表    │
              │ • 商品名称           │
              │ • 库存数量           │
              │ • 操作按钮           │
              │   ✏️ 编辑            │
              │   🖼️ 上传图片        │
              │   🗑️ 删除            │
              └──────────────────────┘
```

---

## 🔐 权限验证流程

```
┌─────────────────────────────────────────┐
│  用户请求访问 /admin/product/new       │
└────────────────────┬────────────────────┘
                     │
                     ▼
         ┌─────────────────────────┐
         │ ProtectedRoute 组件      │
         │ (前端路由保护)           │
         └──────────┬──────────────┘
                    │
        ✅ 用户已登录?
                    │
         ┌──────────┴──────────┐
         │                     │
        否                    是
         │                     │
         ▼                     ▼
    ┌──────────┐    ┌──────────────────┐
    │ 重定向到  │    │ 检查 admin 参数   │
    │ /login   │    │ (from props)      │
    └──────────┘    └────────┬─────────┘
                            │
                     ┌──────┴──────┐
                     │             │
                admin=true   admin=false
                     │             │
                     ▼             ▼
            ┌────────────────┐  ┌─────────┐
            │ 检查用户 role  │  │ 显示    │
            │ (数据库查询)   │  │ Not Found│
            └────────┬───────┘  └─────────┘
                     │
            ┌────────┴────────┐
            │                 │
        role="admin"    role="user"
            │                 │
            ▼                 ▼
       ┌─────────────┐   ┌─────────┐
       │ ✅ 允许访问 │   │ 显示    │
       │ 显示表单    │   │ Not Found│
       └─────────────┘   └─────────┘
```

---

## 📝 详细代码流程

### 阶段 1: 用户注册 (已详细分析，参考另一文档)

```javascript
// 前端: Register.jsx
const submitHandler = (e) => {
  const signUpData = { name, email, password };
  register(signUpData);  // Redux mutation
};

// 后端: authControllers.js → registerUser()
const user = await User.create({ name, email, password });
// 用户被创建，role 默认为 "user"
```

**数据库结果:**
```javascript
{
  _id: ObjectId(...),
  name: "李四",
  email: "lisi@example.com",
  password: "$2a$10$...[hashed]...",
  role: "user",           // ← 普通用户角色
  walletAddress: null,
  tokenBalance: 100,
  createdAt: "2025-11-13T..."
}
```

---

### 阶段 2: 用户登录

```javascript
// 前端: Login.jsx
const submitHandler = (e) => {
  const { email, password } = user;
  login({ email, password });  // Redux mutation
};

// 后端: authControllers.js → loginUser()
export const loginUser = catchAsyncErrors(async (req, res, next) => {
  const { email, password } = req.body;

  // 1️⃣ 验证邮箱和密码不为空
  if (!email || !password) {
    return next(new ErrorHandler("Please enter email & password", 400));
  }

  // 2️⃣ 在数据库中查找用户
  const user = await User.findOne({ email }).select("+password");

  if (!user) {
    return next(new ErrorHandler("Invalid email or password", 401));
  }

  // 3️⃣ 比较密码是否正确
  const isPasswordMatched = await user.comparePassword(password);

  if (!isPasswordMatched) {
    return next(new ErrorHandler("Invalid email or password", 401));
  }

  // 4️⃣ 生成 JWT Token 并返回
  sendToken(user, 200, res);
});
```

**JWT Token 内容:**
```javascript
{
  id: "用户的 MongoDB ObjectId",
  iat: 1234567890,
  exp: 1234654290  // 7 天后过期
}
```

**前端处理:**
```javascript
// authApi.js → onQueryStarted
async onQueryStarted(args, { dispatch, queryFulfilled }) {
  await queryFulfilled;
  // 自动调用 userApi.endpoints.getMe.initiate()
  await dispatch(userApi.endpoints.getMe.initiate(null));
}

// 获取用户信息并保存到 Redux
const user = await User.findById(decodedData.id);
// 返回包含 role 的用户信息
```

**Redux Store 状态:**
```javascript
{
  auth: {
    isAuthenticated: true,
    user: {
      _id: "...",
      name: "李四",
      email: "lisi@example.com",
      role: "user",          // ← 关键字段
      walletAddress: null
    }
  }
}
```

---

### 阶段 3: 前端路由权限检查

```javascript
// 前端: ProtectedRoute.jsx
export default ProtectedRoute = ({ admin, children }) => {
  const { isAuthenticated, user } = useSelector(state => state.auth);

  if (!isAuthenticated) {
    return <Navigate to="/login" />;
  }

  if (admin && user?.role !== "admin") {
    return <Navigate to="/" />;  // 普通用户无法访问
  }

  return children;
};

// 前端: adminRoutes.jsx
<Route
  path="/admin/product/new"
  element={
    <ProtectedRoute admin={true}>
      <NewProduct />
    </ProtectedRoute>
  }
/>
```

**权限检查的三层防护:**
```
第 1 层 (前端客户端): 
  ✅ 已登录? 否 → 跳转到 /login
  ✅ 是 admin? 否 → 跳转到 /

第 2 层 (前端菜单):
  ✅ 仅在用户是 admin 时显示 "发布商品" 菜单

第 3 层 (后端 API):
  ✅ API 路由上的中间件验证
  ✅ isAuthenticatedUser + authorizeRoles("admin")
```

---

### 阶段 4: 用户访问发布商品页面

```javascript
// 前端: Header.jsx 中的菜单
{user?.role === "admin" && (
  <Link to="/admin/dashboard">Dashboard</Link>
  // 然后从 Dashboard 进入 /admin/product/new
)}

// 或直接访问 URL: /admin/product/new
```

**页面加载:**
```javascript
// 前端: NewProduct.jsx
const NewProduct = () => {
  const [product, setProduct] = useState({
    name: "",
    description: "",
    price: "",
    category: "",
    stock: "",
    seller: "",
  });

  const [createProduct, { isLoading, error, isSuccess }] = 
    useCreateProductMutation();

  // 显示表单供用户填写
  return <form onSubmit={submitHandler}>...</form>;
};
```

---

### 阶段 5: 用户填写商品表单

```javascript
// 前端: NewProduct.jsx
const submitHandler = (e) => {
  e.preventDefault();

  // 构造商品数据对象
  const productData = {
    name: "二手 MacBook Pro",
    description: "2020 年款，90% 新",
    price: 150,           // 150 个代币
    category: "Laptops",
    stock: 1,
    seller: "李四的店铺"
  };

  // 调用 Redux mutation 发送给后端
  createProduct(productData);
};
```

---

### 阶段 6: 后端处理商品创建请求

```javascript
// 后端路由: routes/products.js
router
  .route("/admin/products")
  .post(
    isAuthenticatedUser,           // ✅ 检查已登录
    authorizeRoles("admin"),       // ✅ 检查是 admin
    newProduct                     // ✅ 创建商品
  );
```

```javascript
// 后端中间件: auth.js → isAuthenticatedUser
export const isAuthenticatedUser = async (req, res, next) => {
  try {
    // 1️⃣ 从 Cookie 中获取 JWT Token
    const { token } = req.cookies;

    if (!token) {
      return next(new ErrorHandler("Login first", 401));
    }

    // 2️⃣ 验证和解码 Token
    const decodedData = jwt.verify(token, process.env.JWT_SECRET);
    // decodedData = { id: "...", iat: ..., exp: ... }

    // 3️⃣ 从数据库获取用户信息，保存到 req.user
    req.user = await User.findById(decodedData.id);

    if (!req.user) {
      return next(new ErrorHandler("User not found", 404));
    }

    next();
  } catch (error) {
    return next(new ErrorHandler(error.message, 401));
  }
};
```

```javascript
// 后端中间件: auth.js → authorizeRoles
export const authorizeRoles = (...roles) => {
  return (req, res, next) => {
    // 4️⃣ 检查用户角色
    if (!roles.includes(req.user.role)) {
      return next(new ErrorHandler(
        `Role ${req.user.role} is not allowed`,
        403
      ));
    }
    next();
  };
};
```

```javascript
// 后端控制器: controllers/productControllers.js → newProduct()
export const newProduct = catchAsyncErrors(async (req, res) => {
  // 5️⃣ 将当前用户 ID 添加到商品数据
  req.body.user = req.user._id;
  // 现在 req.body = {
  //   name: "二手 MacBook Pro",
  //   description: "2020 年款，90% 新",
  //   price: 150,
  //   category: "Laptops",
  //   stock: 1,
  //   seller: "李四的店铺",
  //   user: ObjectId("...")  ← 商品发布者的 ID
  // }

  // 6️⃣ 创建商品并保存到数据库
  const product = await Product.create(req.body);

  // 7️⃣ 返回创建好的商品给前端
  res.status(200).json({
    product,
  });
});
```

---

### 阶段 7: 数据库保存商品

```javascript
// MongoDB 中的商品集合变化

// 创建前:
db.products.find() // 查询返回已有的商品列表

// 创建后:
{
  _id: ObjectId("649f9999999999999999999a"),
  name: "二手 MacBook Pro",
  description: "2020 年款，90% 新",
  price: 150,
  category: "Laptops",
  stock: 1,
  seller: "李四的店铺",
  user: ObjectId("649e1234567890abcdef1234"),  // ← 李四的 ID
  images: [],
  ratings: 0,
  numOfReviews: 0,
  reviews: [],
  sellerWallet: null,
  sellerUser: ObjectId("649e1234567890abcdef1234"),
  createdAt: "2025-11-13T10:30:00Z",
  updatedAt: "2025-11-13T10:30:00Z"
}
```

---

### 阶段 8: 前端收到响应并跳转

```javascript
// 前端: NewProduct.jsx
const [createProduct, { isLoading, error, isSuccess }] = 
  useCreateProductMutation();

useEffect(() => {
  if (error) {
    toast.error(error?.data?.message);
  }

  if (isSuccess) {
    toast.success("Product created");
    // 8️⃣ 商品创建成功后自动跳转到商品列表页面
    navigate("/admin/products");
  }
}, [error, isSuccess]);
```

---

### 阶段 9: 前端请求获取已发布商品列表

```javascript
// 前端: ListProducts.jsx
const ListProducts = () => {
  // 当组件加载时，自动调用这个查询
  const { data, isLoading, error } = useGetAdminProductsQuery();

  // 这会发送以下请求:
  // GET /api/v1/admin/products
};

// 前端: productsApi.js
getAdminProducts: builder.query({
  query: () => `/admin/products`,
  providesTags: ["AdminProducts"],  // ← 缓存标签
}),
```

---

### 阶段 10: 后端查询所有商品

```javascript
// 后端路由: routes/products.js
router
  .route("/admin/products")
  .get(
    isAuthenticatedUser,           // ✅ 检查已登录
    authorizeRoles("admin"),       // ✅ 检查是 admin
    getAdminProducts               // ✅ 获取商品列表
  );
```

```javascript
// 后端控制器: controllers/productControllers.js → getAdminProducts()
export const getAdminProducts = catchAsyncErrors(async (req, res, next) => {
  // 9️⃣ 从数据库查询所有商品 (无过滤)
  const products = await Product.find();

  res.status(200).json({
    products,  // 返回商品数组
  });
});

// 返回的数据结构:
{
  products: [
    {
      _id: "649f9999999999999999999a",
      name: "二手 MacBook Pro",
      description: "2020 年款，90% 新",
      price: 150,
      stock: 1,
      user: ObjectId("649e1234567890abcdef1234"),
      // ... 其他字段
    },
    {
      _id: "649f8888888888888888888b",
      name: "iPhone 13",
      price: 80,
      stock: 2,
      user: ObjectId("649e5555555555555555555c"),
      // ... 其他字段
    },
    // ... 更多商品
  ]
}
```

---

### 阶段 11: 前端显示商品列表

```javascript
// 前端: ListProducts.jsx
const setProducts = () => {
  const products = {
    columns: [
      { label: "ID", field: "id", sort: "asc" },
      { label: "Name", field: "name", sort: "asc" },
      { label: "Stock", field: "stock", sort: "asc" },
      { label: "Actions", field: "actions", sort: "asc" },
    ],
    rows: [],
  };

  // 遍历每个商品，构建表格行
  data?.products?.forEach((product) => {
    products.rows.push({
      id: product?._id,
      name: `${product?.name?.substring(0, 20)}...`,
      stock: product?.stock,
      actions: (
        <>
          {/* ✏️ 编辑按钮 */}
          <Link
            to={`/admin/products/${product?._id}`}
            className="btn btn-outline-primary"
          >
            <i className="fa fa-pencil"></i>
          </Link>

          {/* 🖼️ 上传图片按钮 */}
          <Link
            to={`/admin/products/${product?._id}/upload_images`}
            className="btn btn-outline-success ms-2"
          >
            <i className="fa fa-image"></i>
          </Link>

          {/* 🗑️ 删除按钮 */}
          <button
            className="btn btn-outline-danger ms-2"
            onClick={() => deleteProductHandler(product?._id)}
            disabled={isDeleteLoading}
          >
            <i className="fa fa-trash"></i>
          </button>
        </>
      ),
    });
  });

  return products;
};

// 使用 MDBDataTable 显示
return (
  <AdminLayout>
    <h1 className="my-5">{data?.products?.length} Products</h1>
    <MDBDataTable data={setProducts()} />
  </AdminLayout>
);
```

**UI 显示效果:**
```
┌─────────────────────────────────────────┐
│  All Products (3 Products)              │
├─────────────────────────────────────────┤
│ ID      │ Name            │ Stock │ Actions  │
├─────────┼─────────────────┼───────┼──────────┤
│ 649f99  │ 二手 MacBook... │  1   │ ✏️ 🖼️ 🗑️  │
├─────────┼─────────────────┼───────┼──────────┤
│ 649f88  │ iPhone 13      │  2   │ ✏️ 🖼️ 🗑️  │
├─────────┼─────────────────┼───────┼──────────┤
│ 649f77  │ AirPods Pro    │  5   │ ✏️ 🖼️ 🗑️  │
└─────────┴─────────────────┴───────┴──────────┘
```

---

## 🎯 关键代码文件映射

| 功能 | 前端文件 | 后端文件 | 核心方法 |
|------|--------|--------|--------|
| **注册登录** | `components/auth/Login.jsx` | `controllers/authControllers.js` | `loginUser()` |
| **权限检查** | `components/auth/ProtectedRoute.jsx` | `middlewares/auth.js` | `isAuthenticatedUser()` <br> `authorizeRoles()` |
| **创建商品表单** | `components/admin/NewProduct.jsx` | `controllers/productControllers.js` | `newProduct()` |
| **商品列表页** | `components/admin/ListProducts.jsx` | `controllers/productControllers.js` | `getAdminProducts()` |
| **Redux API** | `redux/api/productsApi.js` | 路由定义 | `createProduct` <br> `getAdminProducts` |
| **数据模型** | - | `models/product.js` | Product Schema |
| **路由配置** | `components/routes/adminRoutes.jsx` | `routes/products.js` | 定义 API 端点 |

---

## 📊 数据流向总览

```
前端表单输入
    ↓
Redux 创建 mutation 请求
    ↓
POST /api/v1/admin/products
{name, description, price, ...}
    ↓
后端路由中间件 (验证 + 授权)
    ↓
productControllers.newProduct()
    ↓
req.body.user = req.user._id
    ↓
Product.create(req.body)
    ↓
📝 保存到 MongoDB
    ↓
返回 { product: {...} }
    ↓
前端收到 isSuccess = true
    ↓
toast.success() + navigate("/admin/products")
    ↓
GET /api/v1/admin/products
    ↓
后端 productControllers.getAdminProducts()
    ↓
Product.find()
    ↓
📝 从 MongoDB 查询所有商品
    ↓
返回 { products: [...] }
    ↓
前端使用 data.products 构建表格
    ↓
MDBDataTable 显示列表
    ↓
用户可以点击 ✏️ ✏️ 🗑️ 按钮进行后续操作
```

---

## 🔑 关键数据字段解析

### Product 模型字段

```javascript
{
  _id: ObjectId,              // 商品唯一 ID
  name: String,               // 商品名称
  description: String,        // 商品描述
  price: Number,              // 价格 (代币数)
  category: String,           // 分类
  stock: Number,              // 库存数量
  seller: String,             // 卖家名称 (用户输入)
  images: Array,              // 商品图片 (初始为空)
  user: ObjectId,             // ⭐ 发布者用户 ID (关键!)
  sellerWallet: String,       // 卖家钱包地址
  sellerUser: ObjectId,       // 卖家用户 ID
  ratings: Number,            // 评分
  numOfReviews: Number,       // 评论数
  reviews: Array,             // 评论列表
  createdAt: Date,            // 创建时间
  updatedAt: Date,            // 更新时间
}
```

### 关键字段: `user` vs `seller`

```javascript
// 前端提交的数据 (由用户手动输入)
{
  name: "二手 MacBook",
  seller: "李四的店铺",      // ← 用户输入的卖家名称
  price: 150,
  // ...
}

// 后端保存的数据 (自动添加)
{
  name: "二手 MacBook",
  seller: "李四的店铺",       // ← 从前端保留
  user: ObjectId("649e..."),  // ← 后端自动添加 (真实发布者)
  price: 150,
  // ...
}

// 为什么有两个字段?
1. seller (String): 为了显示友好的商店名称
2. user (ObjectId): 为了追踪真实的发布者，验证权限
```

---

## 🚀 完整时间轴

```
T=0秒:    用户登录
T=1秒:    JWT Token 生成和验证
T=2秒:    Redux 获取用户信息 (role="admin")
T=3秒:    Header 显示 "Dashboard" 菜单
T=5秒:    用户点击进入 /admin/product/new
T=6秒:    前端验证 ProtectedRoute (✅ 允许)
T=7秒:    NewProduct 组件加载，显示表单
T=10秒:   用户填写商品信息并提交表单
T=11秒:   POST /api/v1/admin/products 请求
T=12秒:   后端验证 JWT Token (✅ 有效)
T=13秒:   后端检查 role (✅ 是 admin)
T=14秒:   Product.create() 保存到 MongoDB
T=15秒:   返回 HTTP 200 + product 对象
T=16秒:   前端显示 toast.success()
T=17秒:   自动导航到 /admin/products
T=18秒:   GET /api/v1/admin/products 请求
T=19秒:   后端 Product.find() 查询
T=20秒:   MongoDB 返回所有商品数组
T=21秒:   前端 Redux 缓存更新 (tag: "AdminProducts")
T=22秒:   ListProducts 组件加载数据
T=23秒:   构建 MDBDataTable 行数据
T=24秒:   显示商品列表表格

总耗时: 约 24 秒 (大部分是网络请求和数据库操作)
用户感受: ⚡ 大约 1-2 秒看到新页面和列表
```

---

## ⚠️ 重要注意点

### 1️⃣ 当前系统设计的限制

```
❌ 普通用户(role="user")无法发布商品
   原因: 发布商品的 API 路由要求 authorizeRoles("admin")

✅ 只有 admin 用户才能发布
   但系统初始化时通常需要手动修改某个用户的 role

📝 建议的改进方案:
   - 添加 role="seller" (卖家角色)
   - 允许普通用户申请成为卖家
   - 添加卖家权限验证逻辑
```

### 2️⃣ 获取已发布商品的方式

```
当前实现 (getAdminProducts):
✅ 返回所有商品 (Product.find())
❌ 无法区分是哪个用户发布的

改进方案:
// 创建新的查询端点
router.get("/me/products", isAuthenticatedUser, (req, res) => {
  // 返回当前用户发布的商品
  const products = await Product.find({ user: req.user._id });
});
```

### 3️⃣ 权限验证的三层防护

```
第 1 层: 前端路由
- ProtectedRoute 检查 role
- 作用: 快速反馈，改善用户体验
- 缺陷: 可被浏览器 DevTools 绕过

第 2 层: 前端菜单
- 仅显示 admin 用户的菜单项
- 作用: 隐藏不相关的功能
- 缺陷: 可直接输入 URL 绕过

第 3 层: 后端 API
- 中间件检查 JWT Token 和 role
- 作用: 真正的安全保障
- 强度: ⭐⭐⭐⭐⭐ (最安全)

⚠️ 最重要的是第 3 层，前两层仅为优化
```

---

## 📋 查询不同商品的方式对比

| 查询方式 | 调用地方 | 返回内容 | 用途 |
|--------|--------|--------|------|
| `getProducts()` | 首页 `Home.jsx` | 所有商品 (分页) | 用户浏览 |
| `getAdminProducts()` | 后台 `ListProducts.jsx` | 所有商品 (无分页) | 管理员管理 |
| `getProductDetails()` | 商品详情页 | 单个商品详情 | 查看详情 |
| *(建议)* `getMyProducts()` | 用户"我的商品"页面 | 当前用户的商品 | 用户管理自己的商品 |

---

## 🔗 相关流程链接

- 📖 **用户注册登录流程** → 参考 `注册后获得代币流程分析.md`
- 💳 **购买商品流程** → 参考 `购买商品代币交易流程.md`
- 🛠️ **系统配置** → 参考 `Web3配置和部署指南.md`

