# KindShare 购买商品时的代币交易流程

## 概述

本文档说明用户使用虚拟社区货币 (KindToken) 购买商品时，代币如何从买家转账到卖家（管理员）的完整流程。

---

## 📋 完整购买流程

```
用户登录 + 已连接钱包
  ↓
浏览商品列表
  ↓
选择商品 → 点击"购买"按钮
  ↓
[前端] 将商品加入购物车
  ↓
跳转到购物车页面
  ↓
点击"结账" 按钮
  ↓
选择配送信息
  ↓
确认订单
  ↓
选择支付方式 (虚拟代币支付)
  ↓
[前端] 发起支付交易
  ↓
[后端] 处理订单
  ↓
[Web3] 执行代币转账
  ↓
[区块链] 交易确认
  ↓
[前端] 显示支付成功
  ↓
[后端] 更新订单状态
  ↓
✅ 用户代币减少，管理员代币增加
```

---

## 🔑 详细步骤分析

### 步骤 1-4：商品选择和购物车

**文件：** `frontend/src/components/product/ProductItem.jsx`

```javascript
// 前端显示商品卡片
const ProductItem = ({ product }) => {
  return (
    <div className="product-card">
      <img src={product.images[0]} alt={product.name} />
      <h5>{product.name}</h5>
      <p>价格: {product.price} KindToken</p>
      
      {/* 购买按钮 */}
      <button onClick={() => addToCart(product)}>
        购 买
      </button>
    </div>
  );
};
```

**状态：** Redux Store 中的 cart 状态更新
```javascript
{
  cartItems: [
    {
      product: "649e...", 
      name: "二手手机",
      price: 50,              // 价格：50 个代币
      quantity: 1,
      image: "...",
      seller: "admin_user_id" // 卖家是谁（可能是管理员或其他用户）
    }
  ]
}
```

---

### 步骤 5-8：结账信息收集

**文件：** `frontend/src/components/cart/Shipping.jsx`、`ConfirmOrder.jsx`

```javascript
// 用户填写收货信息
const shippingInfo = {
  address: "北京市朝阳区",
  city: "北京",
  phoneNo: "13800138000",
  zipCode: "100000",
  country: "China"
};

// 订单总金额
const totalAmount = 50;  // 50 个 KindToken
```

---

### 步骤 9：选择支付方式

**文件：** `frontend/src/components/cart/PaymentMethod.jsx`

```javascript
const PaymentMethod = () => {
  const [paymentMethod, setPaymentMethod] = useState("Card");
  
  return (
    <div>
      <h4>选择支付方式</h4>
      
      <div className="form-check">
        <input 
          type="radio" 
          value="Card" 
          onChange={(e) => setPaymentMethod("Card")}
        />
        <label>信用卡 (Stripe)</label>
      </div>
      
      <div className="form-check">
        <input 
          type="radio" 
          value="Crypto" 
          onChange={(e) => setPaymentMethod("Crypto")}
          checked
        />
        <label>✨ 虚拟代币 (KindToken) ← 选择这个</label>
      </div>
    </div>
  );
};
```

---

### 步骤 10：发起支付交易

**文件：** `frontend/src/components/cart/ConfirmOrder.jsx`

```javascript
const handlePlaceOrder = async () => {
  try {
    // 验证用户是否已连接钱包
    if (!user.walletAddress) {
      toast.error("请先连接钱包");
      return;
    }

    // 构造订单数据
    const orderData = {
      orderItems: cartItems,
      shippingInfo: shippingInfo,
      itemsPrice: itemsPrice,
      taxAmount: taxAmount,
      shippingAmount: shippingAmount,
      totalAmount: totalAmount,
      paymentMethod: "Crypto",  // ← 虚拟代币支付
      paymentInfo: {
        method: "Web3Transfer",
        status: "pending"
      }
    };

    // 发起订单
    const response = await newOrder(orderData);
    
    if (paymentMethod === "Crypto") {
      // 🔄 执行代币转账
      await handleWeb3Payment(orderData);
    }
  } catch (error) {
    toast.error("订单创建失败");
  }
};

// Web3 代币转账
const handleWeb3Payment = async (orderData) => {
  try {
    // 获取管理员钱包地址
    const adminWalletAddress = await getAdminWallet();
    
    // 调用代币转账 API
    const result = await transferTokens({
      toAddress: adminWalletAddress,
      amount: orderData.totalAmount
    });

    if (result.success) {
      toast.success("支付成功！");
      // 清空购物车
      clearCart();
      // 跳转到订单详情
      navigate(`/order/${response.order._id}`);
    }
  } catch (error) {
    toast.error("支付失败: " + error.message);
  }
};
```

---

### 步骤 11-12：后端处理订单

**文件：** `backend/controllers/orderControllers.js`

```javascript
export const newOrder = catchAsyncErrors(async (req, res, next) => {
  const {
    orderItems,
    shippingInfo,
    itemsPrice,
    taxAmount,
    shippingAmount,
    totalAmount,
    paymentMethod,
    paymentInfo,
  } = req.body;

  // 📝 第 1 步: 创建订单记录
  const order = await Order.create({
    orderItems,           // 购买的商品
    shippingInfo,         // 收货信息
    itemsPrice,           // 商品总价
    taxAmount,            // 税费
    shippingAmount,       // 运费
    totalAmount,          // 总金额 (50 个代币)
    paymentMethod,        // "Crypto"
    paymentInfo,          // 支付信息
    user: req.user._id,   // 买家用户 ID
    orderStatus: "Processing"  // 订单状态
  });

  res.status(200).json({
    order,
  });
});
```

**数据库变化（MongoDB）：**

```javascript
// 创建新订单文档
{
  _id: ObjectId("12345..."),
  user: ObjectId("buyer_id"),          // 买家
  orderItems: [
    {
      product: ObjectId("649e..."),
      name: "二手手机",
      price: 50,
      quantity: 1,
      image: "..."
    }
  ],
  shippingInfo: {
    address: "北京市朝阳区",
    city: "北京",
    phoneNo: "13800138000",
    zipCode: "100000",
    country: "China"
  },
  itemsPrice: 50,
  taxAmount: 0,
  shippingAmount: 0,
  totalAmount: 50,
  paymentMethod: "Crypto",
  paymentInfo: {
    method: "Web3Transfer",
    status: "pending"
  },
  orderStatus: "Processing",
  deliveredAt: null,
  createdAt: "2025-11-12T10:30:00Z",
  updatedAt: "2025-11-12T10:30:00Z"
}
```

---

### 步骤 13-14：Web3 代币转账

**文件：** `backend/controllers/web3Controllers.js`

```javascript
export const transferTokensByUser = catchAsyncErrors(async (req, res, next) => {
  const { toAddress, amount } = req.body;
  // toAddress = "0x管理员钱包地址"
  // amount = 50 (代币数量)

  // 🔍 第 1 步: 获取当前用户信息
  const user = await User.findById(req.user._id);

  if (!user.walletAddress) {
    return next(new ErrorHandler("User wallet not connected", 400));
  }

  // 🔍 第 2 步: 验证收方钱包地址
  if (!isValidEthereumAddress(toAddress)) {
    return next(new ErrorHandler("Invalid recipient address", 400));
  }

  // 🔍 第 3 步: 验证转账金额
  if (!amount || amount <= 0) {
    return next(new ErrorHandler("Amount must be greater than 0", 400));
  }

  // 🔍 第 4 步: 检查买家余额
  const balance = await getTokenBalance(user.walletAddress);
  if (parseFloat(balance) < amount) {
    return next(new ErrorHandler("Insufficient token balance", 400));
  }

  // ✨ 第 5 步: 执行代币转账
  const result = await transferTokens(
    toAddress,      // 管理员钱包
    amount          // 50 个代币
  );

  if (!result.success) {
    return next(new ErrorHandler(result.error, 500));
  }

  // 📝 第 6 步: 返回成功响应
  res.status(200).json({
    success: true,
    transactionHash: result.transactionHash,
    message: `Successfully transferred ${amount} tokens`
  });
});
```

---

### 步骤 15：智能合约执行转账

**文件：** `backend/utils/web3Service.js`

```javascript
export const transferTokens = async (toAddress, amount) => {
  try {
    // 🔧 第 1 步: 获取智能合约实例
    const contract = getContract();

    // 🔧 第 2 步: 将金额转换为 Wei
    const amountInWei = ethers.parseUnits(amount.toString(), 18);
    // amount = 50 → amountInWei = 50000000000000000000 Wei

    console.log(`Transferring ${amount} tokens to ${toAddress}`);

    // 🔧 第 3 步: 调用智能合约的 transfer() 函数
    const tx = await contract.transfer(
      toAddress,      // 管理员钱包: "0x..."
      amountInWei     // 50 * 10^18 Wei
    );

    // 🔧 第 4 步: 等待交易确认
    const receipt = await tx.wait();

    console.log(`Transaction hash: ${receipt.hash}`);

    // 🔧 第 5 步: 返回交易结果
    return {
      success: true,
      transactionHash: receipt.hash,
      blockNumber: receipt.blockNumber
    };
  } catch (error) {
    console.error("Transfer error:", error);
    return {
      success: false,
      error: error.message
    };
  }
};
```

---

### 步骤 16：区块链执行转账

**区块链 (Sepolia) 上发生的变化：**

```javascript
// 转账前状态
balanceOf(买家钱包 0x742d...) = 100 个 KindToken
balanceOf(管理员钱包 0xabcd...) = 50 个 KindToken

// transfer() 交易执行
contract.transfer(
  toAddress: "0xabcd...",
  amount: 50000000000000000000 Wei
)

// 转账后状态
balanceOf(买家钱包 0x742d...) = 50 个 KindToken  ✅ 减少 50
balanceOf(管理员钱包 0xabcd...) = 100 个 KindToken ✅ 增加 50

// 区块链事件日志
emit Transfer(
  from: "0x742d..." (买家),
  to: "0xabcd..." (管理员),
  value: 50000000000000000000 (50 个代币)
)
```

---

### 步骤 17-18：前端显示结果

**文件：** `frontend/src/components/cart/ConfirmOrder.jsx`

```javascript
// 支付成功后的处理
if (result.success) {
  // 显示成功提示
  toast.success("✅ 支付成功！");
  
  // 记录交易哈希
  console.log("Transaction Hash:", result.transactionHash);
  // Output: "0x123abc...xyz"
  
  // 清空购物车
  dispatch(clearCart());
  
  // 更新用户信息（重新获取钱包余额）
  const updatedBalance = await getTokenBalance(user.walletAddress);
  // updatedBalance = "50"  (100 - 50 = 50)
  
  // 显示新的代币余额
  toast.info(`您的代币余额: ${updatedBalance}`);
  
  // 跳转到订单确认页面
  navigate(`/me/orders/${order._id}`);
}
```

---

## 📊 数据流和状态变化

### 用户 (买家) 状态变化

```javascript
// 购买前
买家用户对象 {
  _id: "buyer_id",
  name: "张三",
  email: "user@example.com",
  walletAddress: "0x742d35Cc6634C0532925a3b844Bc9e7595f...",
  tokenBalance: 100  // MongoDB 中的值
}

买家 MetaMask 钱包 {
  address: "0x742d...",
  balanceOf("KindToken") = 100  // 区块链中的真实值
}

// 购买后
买家用户对象 {
  _id: "buyer_id",
  name: "张三",
  email: "user@example.com",
  walletAddress: "0x742d35Cc6634C0532925a3b844Bc9e7595f...",
  tokenBalance: 100  // MongoDB 中的值（可能不会自动更新）
}

买家 MetaMask 钱包 {
  address: "0x742d...",
  balanceOf("KindToken") = 50  // ✅ 区块链已更新
}
```

### 管理员 (卖家) 状态变化

```javascript
// 购买前
管理员用户对象 {
  _id: "admin_id",
  name: "管理员",
  email: "admin@example.com",
  walletAddress: "0xabcd12345678abcd12345678abcd12345678abcd",
  tokenBalance: 50
}

管理员 MetaMask 钱包 {
  address: "0xabcd...",
  balanceOf("KindToken") = 50
}

// 购买后
管理员用户对象 {
  _id: "admin_id",
  name: "管理员",
  email: "admin@example.com",
  walletAddress: "0xabcd12345678abcd12345678abcd12345678abcd",
  tokenBalance: 50  // MongoDB 中的值（可能不会自动更新）
}

管理员 MetaMask 钱包 {
  address: "0xabcd...",
  balanceOf("KindToken") = 100  // ✅ 区块链已更新
}
```

### 订单记录

```javascript
// MongoDB 中的订单文档
{
  _id: ObjectId("order_id"),
  user: ObjectId("buyer_id"),  // 买家
  orderItems: [...],
  totalAmount: 50,              // 支付金额：50 个代币
  paymentMethod: "Crypto",
  paymentInfo: {
    transactionHash: "0x123abc...xyz",  // 区块链交易哈希
    status: "completed"
  },
  orderStatus: "Processing",
  createdAt: "2025-11-12T10:30:00Z"
}
```

---

## 🔄 完整的代币流动图

```
初始状态:
┌─────────────┐           ┌──────────────┐
│   买家      │           │   管理员     │
│  (0x742d..) │           │  (0xabcd..)  │
│             │           │              │
│  100 代币   │           │  50 代币     │
└─────────────┘           └──────────────┘


购买商品:
买家点击"购买" → 发起 50 代币的转账交易

           转账交易
           ↓
      [区块链执行]
           ↓

转账后状态:
┌─────────────┐           ┌──────────────┐
│   买家      │           │   管理员     │
│  (0x742d..) │           │  (0xabcd..)  │
│             │           │              │
│  100 - 50   │           │  50 + 50     │
│  = 50 代币  │  ◄─────►  │  = 100 代币  │
│             │           │              │
└─────────────┘           └──────────────┘

结果:
✅ 买家: 100 → 50 (花费 50 个代币购买商品)
✅ 管理员: 50 → 100 (收到 50 个代币)
✅ 订单: 已创建并标记为"已支付"
```

---

## 📋 流程中的关键文件和函数

| 步骤 | 文件位置 | 函数名 | 操作 |
|------|--------|------|------|
| 1️⃣ | 前端 Product | handleAddToCart() | 商品加入购物车 |
| 2️⃣ | 前端 Cart | handleCheckout() | 进入结账流程 |
| 3️⃣ | 前端 Shipping | handleShippingSubmit() | 提交收货信息 |
| 4️⃣ | 前端 ConfirmOrder | handlePlaceOrder() | 确认订单 |
| 5️⃣ | 后端 orderControllers | newOrder() | 创建订单（MongoDB） |
| 6️⃣ | 后端 web3Controllers | transferTokensByUser() | 验证转账条件 |
| 7️⃣ | 后端 web3Service | transferTokens() | 调用智能合约 |
| 8️⃣ | 智能合约 | transfer() | 执行代币转账 |
| 9️⃣ | 区块链 | 挖矿确认 | 交易被确认 |
| 🔟 | 前端 | showSuccessMessage() | 显示支付成功 |

---

## ⚠️ 重要注意点

### 1. 代币转账的实际执行者

```
❌ 不是这样：
用户的 MetaMask 直接进行转账
(用户需要支付 Gas 费用)

✅ 是这样：
后端使用自己的私钥进行转账
(后端支付 Gas 费用)

为什么?
- 用户可能没有 ETH (Sepolia 测试币) 来支付 Gas 费用
- 更好的用户体验
- 后端可以集中管理交易
```

### 2. MongoDB 中的 tokenBalance 和区块链中的余额可能不同步

```
MongoDB:
user.tokenBalance = 100  (可能过期，不自动更新)

区块链 (Sepolia):
balanceOf(user.walletAddress) = 50  (实时准确)

前端应该:
✅ 从区块链查询实时余额，而不是从数据库
// 正确的做法
const balance = await getTokenBalance(user.walletAddress);

❌ 不要只依赖数据库
// 错误的做法
const balance = user.tokenBalance;
```

### 3. 交易不可逆

```
一旦交易被确认:
- 代币转账完成
- 无法撤销（除非管理员手动干预）
- 用户需要仔细检查转账金额

所以需要:
✅ 在转账前显示确认对话框
✅ 验证用户有足够的代币
✅ 防止误操作
```

### 4. Gas 费用的管理

```
// 后端钱包需要保持足够的 Sepolia ETH
// 来支付 Gas 费用

后端钱包:
- 初始: 1.0 Sepolia ETH
- 每次转账消耗 Gas: ~0.001-0.003 ETH
- 需要定期充值，否则交易会失败

监控 Gas 价格:
const gasPrice = await provider.getGasPrice();
const estimatedGas = 50000;  // 平均 Gas 用量
const totalCost = gasPrice * estimatedGas;
```

---

## 🚀 购买流程总结

| 阶段 | 执行者 | 关键操作 | 结果 |
|------|------|--------|------|
| **选购** | 前端 | 添加商品到购物车 | cartItems 更新 |
| **结账** | 前端 | 收集订单信息 | 订单数据准备好 |
| **验证** | 后端 | 验证钱包和余额 | 确保可以支付 |
| **执行** | 区块链 | 转账代币 | 买家 -50, 卖家 +50 |
| **确认** | 前端 | 显示成功消息 | 用户看到新余额 |

---

## 📝 API 请求和响应示例

### 1. 创建订单

```javascript
// 请求
POST /api/v1/orders/new
Content-Type: application/json
Cookie: token=eyJhbGc...

{
  "orderItems": [
    {
      "product": "649e1234567890abcdef1234",
      "name": "二手手机",
      "price": 50,
      "quantity": 1,
      "image": "..."
    }
  ],
  "shippingInfo": {
    "address": "北京市朝阳区",
    "city": "北京",
    "phoneNo": "13800138000",
    "zipCode": "100000",
    "country": "China"
  },
  "itemsPrice": 50,
  "taxAmount": 0,
  "shippingAmount": 0,
  "totalAmount": 50,
  "paymentMethod": "Crypto",
  "paymentInfo": {
    "method": "Web3Transfer",
    "status": "pending"
  }
}

// 响应 200 OK
{
  "order": {
    "_id": "12345abc...",
    "user": "buyer_id",
    "orderItems": [...],
    "totalAmount": 50,
    "orderStatus": "Processing",
    "createdAt": "2025-11-12T10:30:00Z"
  }
}
```

### 2. 转账代币

```javascript
// 请求
POST /api/v1/tokens/transfer
Content-Type: application/json
Cookie: token=eyJhbGc...

{
  "toAddress": "0xabcd12345678abcd12345678abcd12345678abcd",
  "amount": 50
}

// 响应 200 OK
{
  "success": true,
  "transactionHash": "0x123abc...xyz",
  "message": "Successfully transferred 50 tokens"
}

// 前端可以用 transactionHash 在 Sepolia Etherscan 查询交易
// https://sepolia.etherscan.io/tx/0x123abc...xyz
```

---

## 🔗 相关文件导航

```
购买流程涉及的文件:

前端 (React):
├── src/components/product/ProductItem.jsx ........... 商品卡片
├── src/components/cart/Cart.jsx ..................... 购物车
├── src/components/cart/Shipping.jsx ................ 收货信息
├── src/components/cart/ConfirmOrder.jsx ............ 订单确认
├── src/components/cart/PaymentMethod.jsx .......... 支付方式
└── src/redux/api/orderApi.js ....................... 订单 API

后端 (Node.js):
├── controllers/orderControllers.js ................. newOrder()
├── controllers/web3Controllers.js .................. transferTokensByUser()
├── utils/web3Service.js ............................ transferTokens()
├── models/order.js ................................ 订单模型
├── routes/order.js ................................ 订单路由
└── routes/web3.js ................................. Web3 路由

智能合约:
└── backend/contracts/KindToken.sol ................ ERC-20 合约

区块链:
└── Sepolia 测试网络 ............................... 交易执行环境
```

